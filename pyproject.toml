[project]
name = "nick-cave"
version = "0.1.0"
description = "Nick Cave lyric search with Flask"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.2",
    "gunicorn>=23.0.0",
]
